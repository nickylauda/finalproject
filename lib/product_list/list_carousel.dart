import 'package:flutter/material.dart';

class ListCarousel extends StatefulWidget {
  const ListCarousel({super.key});

  @override
  State<ListCarousel> createState() => _ListCarouselState();
}

class _ListCarouselState extends State<ListCarousel> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Column(),);
  }
}