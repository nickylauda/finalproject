import 'package:flutter/material.dart';

class ListSponsoredAdds extends StatefulWidget {
  const ListSponsoredAdds({super.key});

  @override
  State<ListSponsoredAdds> createState() => _ListSponsoredAddsState();
}

class _ListSponsoredAddsState extends State<ListSponsoredAdds> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Column(),);
  }
}